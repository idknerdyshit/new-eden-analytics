# Domain & TLS
DOMAIN=localhost
ACME_EMAIL=you@example.com

# Database
DATABASE_URL=postgres://nea:nea_dev@timescaledb:5432/new_eden_analytics
POSTGRES_USER=nea
POSTGRES_PASSWORD=nea_dev
POSTGRES_DB=new_eden_analytics

# EVE SSO
ESI_CLIENT_ID=
ESI_CLIENT_SECRET=
ESI_CALLBACK_URL=http://localhost:3000/auth/callback

# Session — must be ≥ 32 chars and not a default value when DOMAIN != localhost
SESSION_SECRET=change-me-to-a-random-string-at-least-32-chars

# Logging
RUST_LOG=info
